package rest

// registerAgentRoutes wires the AI agent worker endpoints. Implemented in
// the AI agent task; kept as a hook so route registration stays in one
// place.
func (s *Server) registerAgentRoutes() {}
